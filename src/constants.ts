// Internal PsiNMR manual (served by the app itself; see
// src/demo/psinmr/DocsView.tsx). Legacy NMRium doc paths appended to this
// base are mapped onto the internal pages.
export const docsBaseUrl = '#/docs';
